{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set, scoped to this app's own webview. The renderer gets no filesystem, shell, or network access; every privileged action goes through an explicit command. Scoped by `webviews` rather than `windows` on purpose: Tauri grants a window-scoped capability to every webview in that window, which would hand IPC to a page the Browser app loaded from the internet.",
  "webviews": ["main"],
  "permissions": ["core:default"]
}
